# 개인정보 보호 및 보안

본 서비스를 개발하는 Lightframe.(이하 권율)은 개인의 기억, 취향, 가치관, 대화와 교정 기록을 다루는 만큼, 자연스러운 AI 경험과 편리한 기능만큼이나 사용자의 데이터와 Persona의 안전성을 중요하게 생각합니다.

개인정보 보호와 보안은 Lightframe.와 Light Intelligence가 매우 중요하게 생각하는 핵심 원칙이기에, README의 일부 항목으로만 다루지 않고 별도의 문서로 분리했습니다. 기능을 추가하거나 AI Provider를 확장할 때도 이 원칙을 뒤로 미루지 않고, 모든 설계와 기술적 결정에서 우선적으로 검토하겠다는 의미를 담고 있습니다.

이 문서는 Light Intelligence가 개인정보와 보안을 어떻게 다루는지, 그리고 왜 그렇게 설계하는지를 설명합니다. 법률 약관을 대신하는 문서가 아니라, Light Intelligence가 지키는 설계 기준을 투명하게 설명합니다.

---

## 필요한 정보만, 필요한 순간에 사용합니다.

Light Intelligence는 Personal AI 경험을 제공하기 위해 필요한 정보만 다룹니다. 대화 기록, Persona 설정, 교정 기록, 사용자가 직접 승인한 Persona 예시와 Local 위키 문서는 사용자가 서비스를 자신에게 맞게 이용하기 위해 저장될 수 있습니다.

Light Intelligence는 기능과 관계없는 개인정보를 요구하거나 수집하지 않습니다. 대화와 위키에는 비밀번호, 인증 정보, 금융 정보, API Key와 같이 노출 시 피해가 발생할 수 있는 민감한 정보나 Secret을 입력하지 않는 것을 권장합니다.

## 기본은 사용자의 기기 안에서 시작합니다.

현재 Light Intelligence의 대화 기록, 설정, 교정 기록, 검토된 Persona 예시와 Local 위키 문서는 기본적으로 사용 중인 브라우저의 `localStorage`에 저장됩니다.

이 데이터는 별도의 사용자 계정 데이터베이스에 자동으로 업로드되지 않습니다. 브라우저의 사이트 데이터를 삭제하면 해당 기기에 저장된 Light Intelligence 데이터도 함께 삭제될 수 있습니다.

Light Intelligence는 사용자의 정보를 서버에 모으기보다, 가능한 한 사용자의 기기 안에서 처리하는 방식을 우선합니다.

## 외부 AI 연결은 답변 생성이 필요할 때만 이루어집니다.

Gemini가 설정된 경우 AI 답변을 만들기 위해 현재 대화의 최근 메시지, Persona 표현 강도 설정과 사용자가 직접 승인한 최근 Persona 예시가 Google Gemini API로 전송될 수 있습니다.

Local 위키 문서는 Gemini에 자동으로 전송하지 않습니다. `율 같음` 또는 교정 저장을 통해 사용자가 직접 승인한 Persona 예시만 다음 답변을 만드는 과정에 제한적으로 반영합니다.

외부 AI Provider로 전송되는 정보는 해당 Provider의 정책에 따라 처리될 수 있습니다. 따라서 민감한 개인정보나 Secret은 AI 대화에 입력하지 않는 것을 원칙으로 합니다.

## Persona는 확인된 정보와 사용자의 승인을 우선합니다.

Light Intelligence는 실제 권율 본인이 아니라, 율이 직접 확인한 정보를 바탕으로 만든 AI Persona입니다.

- 실제 권율 본인인 것처럼 가장하지 않습니다.
- 확인되지 않은 기억, 감정, 취향과 개인정보를 사실처럼 생성하지 않습니다.
- 알 수 없는 내용은 추측으로 채우지 않고 모르는 상태로 남깁니다.
- 민감한 개인정보를 묻거나 공개하도록 요청하는 질문에는 답변하지 않습니다.
- 사용자가 직접 확인하거나 교정한 Persona 예시만 다음 답변에 반영합니다.

Persona 예시는 저장하기 전에 질문과 답변을 확인할 수 있으며, 저장된 예시는 사용자가 직접 삭제할 수 있습니다.

## 사용자는 자신의 데이터를 통제합니다.

Light Intelligence의 로컬 데이터는 사용자가 사용하는 브라우저에 저장됩니다. 사용자는 서비스의 데이터 관리 기능이나 브라우저의 사이트 데이터 삭제 기능을 통해 자신의 데이터를 직접 관리할 수 있습니다.

Local 위키에 표시되는 `나만 보기`, `친한 사람` 등의 공개 범위는 현재 문서를 분류하기 위한 정보입니다. 사용자 인증이나 서버 기반의 실제 접근 제어를 제공하는 기능은 아닙니다.

계정, Cloud 동기화, 분석 기능처럼 데이터 처리 방식에 영향을 주는 기능을 추가하는 경우에는 수집 항목, 사용 목적, 전달 대상, 보관 위치와 삭제 방식을 이 문서에 명확히 업데이트합니다.

## 비밀 정보는 공개 코드에 두지 않습니다.

Gemini API Key와 서비스 운영에 필요한 비밀값은 Client 코드나 Git 저장소에 포함하지 않습니다. 실제 API Key는 Server 환경 변수에서만 읽으며, Client Bundle에 노출하지 않습니다.

공개 저장소에는 예시 환경 변수와 공개해도 안전한 구성만 포함합니다. 중요한 권한을 가진 Key와 개인 인증 정보는 Server에서만 사용합니다.

Chat API는 동일 출처 요청을 확인하고, 입력 길이와 최근 대화 수를 제한하며, 과도한 호출에 기본 Rate Limit을 적용합니다.

## 안정성과 신뢰성은 AI 기능의 일부입니다.

Light Intelligence는 자연스러운 답변뿐 아니라, 예측 가능하고 신뢰할 수 있는 Personal AI를 지향합니다.

- AI가 답변에 사용한 Self Model 영역과 근거 수준을 가능한 한 명확히 보여줍니다.
- 확인된 정보와 추측을 구분합니다.
- 자동 학습보다 사용자의 검토와 승인을 우선합니다.
- 어떤 정보가 기기에 저장되고 외부 AI에 전달되는지 명확히 설명합니다.
- 개인정보 보호와 보안을 나중에 덧붙이는 기능이 아니라 Persona 설계의 기본 조건으로 다룹니다.

## Lightframe. 우리의 기준.

> UI는 사용자가 서비스를 신뢰하게 되는 첫 경험입니다.  
> UX는 그 경험이 자연스럽게 이어지도록 만듭니다.  
> 개인정보 보호와 보안은 사용자가 보지 못하더라도 절대 타협하지 않는 기반입니다.

Light Intelligence는 개인을 흉내 내는 AI가 아니라, 사용자가 직접 확인한 자신을 안전하게 이해하고 확장하기 위해 존재합니다. 그 과정에서 자연스러움과 편리함이 사용자의 개인정보, 선택권과 신뢰를 희생해서 만들어져서는 안 된다고 믿습니다.
